# Ecosystem audit — 2026-07-10 (first full-registry run)

First `pm report` across the **full** target registry after Wave 0 registered the 5 previously-invisible
repos (openarms, openfleet, devops-control-plane, narrative-to-neutral-report-transformer,
warp-solar-system-shaders). Machine-readable snapshot: `2026-07-10-ecosystem-audit.json`.

Generated by: `python3 -m tools.pm report` (read-only; no target mutated). Registry: 10 targets
(project-maintainer is the tool, not a target of itself).

## Consolidated map — 469 findings across 10 targets

| Target | Findings | Notable breakdown | Policy |
|---|---:|---|---|
| second-brain | 267 | broken-link + trailing-ws across the 1,035-page corpus | preserve · **READ-ONLY** (surfaced only, never mutated) |
| openfleet | 122 | broken-link 82 (**25 stale `research-wiki` rename** + 3 `-home-jfortin-openfleet/memory/` auto-debris + ~54 internal doc drift), slur 6, trailing-ws 34 | preserve |
| narrative-to-neutral-report-transformer | 29 | trailing-ws 28, vulgar 1 (`bastard`, flag-only) | flag-only |
| openarms | 22 | broken-link + trailing-ws | preserve |
| selfdef | 11 | — | flag-only |
| root-ghostproxy | 9 | — | preserve |
| sovereign-os | 5 | — | flag-only |
| the-virus-block-mc | 3 | — | flag-only |
| devops-control-plane | 1 | trailing-ws 1 | flag-only |
| warp-solar-system-shaders | 0 | clean (1-line README) | flag-only |

## What this surfaces (Wave 0 findings, operator-gated)

1. **openfleet's stale cross-project rename is real and empirically confirmed.** 25 broken links (plus its
   `.mcp.json` + `tools/gateway.py` forwarder) point at the OLD name `devops-solutions-research-wiki`
   (now `devops-solutions-information-hub`), all via `/home/jfortin/` absolute paths — the operator's real
   machine, unverifiable from a container. This is a repo-wide rename touching brain/config, NOT a one-line
   fix. **Needs operator confirmation of the real machine layout before any change** (does their machine
   clone the brain as `research-wiki` or `information-hub`?). openfleet Hard Rule 3 forbids blind config edits.
2. **The trailing-ws findings are mechanical `pm clean` territory** (NNRT 28, openfleet 34, sovereign-os,
   selfdef, dcp…). Cheap to `--apply` — but per project-maintainer rules, any `--apply` against a target
   needs operator approval. Surfaced, not applied.
3. **The slur/vulgar flags are policy-gated and preserved** (openfleet slur 6 under `preserve` = flag-only,
   NNRT `bastard` under `flag-only`). Operator decides; the profanity fixer refuses under these policies.
4. **second-brain (267) is surfaced-only** — READ-ONLY target per AGENTS.md Hard Rule 6; never mutated from here.

## The lever status change

Before Wave 0: registry covered 5/11 repos; `pm report` had run against ~1 real target in history.
After Wave 0: registry covers all 10 maintainable targets; this is the first ecosystem-wide state map.
The toolbelt now sees the whole ecosystem instead of ~half of it.
