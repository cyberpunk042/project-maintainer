# 2026-07-03 — Operator init directive (verbatim, sacrosanct)

> Logged BEFORE acting, per ecosystem Hard Rule (operator words are sacrosanct — quote verbatim, never paraphrase).

## Operator directive (verbatim)

> "Lets init and start working on project-maintainer,  on the main. Do the base and ask questions if needed, e.g. of things it will do; clean the wiki and docs folders, corrections, second-brain/wiki-hub implantation or upgrade and etc... you can look at the second-brain for help too. right now its new and empty, it will help us to clean the other project with scripts and such, template and such, migrations, etc..."

## Scope answers (operator, via question round — verbatim where given)

- Tool stack: **"Mixed (Python core + bash wrappers)"**
- Capabilities: **"all this and more... think about the name and extrapole"** + Wiki/docs cleaner + Brain implanter/upgrader + Migration runner + Template library
- Target mode: **"Both from day one"** (path-based core + registry layer)

## Agent reading (flagged as agent-interpretation, not operator words)

- Work happens on `main` (operator: "on the main").
- The project's name is the spec: **maintain, clean, level-up projects**. Extrapolated verb set: audit / clean / fix / implant / upgrade / migrate / scaffold / report — see CLAUDE.md routing.
- The second-brain (`devops-solutions-information-hub`) is the reference: adoption guide + methodology engine + templates are consumed from there, adapted here.
