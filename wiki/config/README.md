# wiki/config/ — methodology config

`methodology.yaml` is a copy of the second-brain's engine (adoption guide step 1: "Copy and adapt"). Invariants kept as-is (stage names, ordering, readiness ranges); project-specific gate commands for THIS repo are `python3 -m tools.pm selfcheck` + dry-run verb passes against a fixture target. Adaptation of stage artifacts to this project's shape happens as M002+ work touches each stage.
